'use client'

import { createTheme } from '@mui/material/styles';

export const colorTheme = createTheme({
  palette: {
    sage: {
      main: '#9CAF88',
      light: '#b5c4a6',
      dark: '#768c5e',
      contrastText: '#221C35',
    },
  },
});
